# API key secret token shared between the daemon and remote server
API_KEY=your_mock_secret_token_here

# Note: LLM_MODEL, LLM_API_KEY, and LLM_API_BASE have moved to the local daemon's /setup page
# and are held securely on each local client (per-school multi-tenant credentials).
# There is no longer any need to configure LLM settings on the remote server side.
